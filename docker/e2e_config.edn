{:port 3005
 :bind-host "0.0.0.0"
 :dev? true
 :e2e? true
 :db {}
 ;; In docker, semsearch talks to the `ollama` sidecar service. Host-mode
 ;; e2e (test/e2e_config.edn) uses 127.0.0.1 instead.
 :semsearch {:ollama-url "http://ollama:11434"
             :ollama-model "nomic-embed-text"}}
