.DS_Store
.lsp
public/imgs
.calva
.cpcache    
.nrepl-port
node_modules
resources/public/js/cljs-runtime
.shadow-cljs
resources/public/js/main.js
resources/public/js/manifest.edn
target
server.jar
config.edn
config.edn.bkp
logs
*.jks
*.pkcs12
*.pem
files/
resources/public/imgs
.lsp/*
!.lsp/
!.lsp/config.edn
.clj-kondo/*
!.clj-kondo/
!.clj-kondo/config.edn
!.clj-kondo/hooks/
!.clj-kondo/hooks/*
rhizome.db
.features-gen
test-results
playwright-report
docker-rhizome/token
# Belt and braces for any stray OAuth token: the rule above names a directory
# that does not exist, so on its own it protects nothing. This repo is public
# -- never let a file by that name become addable, wherever it turns up.
token
docker/token
.playwright-mcp
# Playwright's compiled-test cache, moved in here out of the shared system temp
# dir by scripts/e2e.sh -- which also drops it before every run. See the comment
# there for what a stale one did.
.playwright-cache
rhizome-test.db
rhizome-e2e.db
*.db-journal
*.db-wal
*.db-shm
.sqlite-vec
tracker
claude-stuff
settings.local.json
.claude/scheduled_tasks.lock
.dev-server.lock
.shadow-cljs.pid
# Generated by scripts/write-compose-ports.sh on every `make box`.
docker/compose.ports.yml
.envrc
nohup.out
# Per-machine marker: its presence makes this checkout the primary (the one
# instance allowed to write). Excluded from the owner's sync by the .nosync
# suffix, and never committed -- see config/read-only-replica?.
primary.nosync
CLAUDE.md
handoffs
plans
scratchpad
screenshots